export type Endpoints = {
  archived: string;
  sent: string;
  booked: string;
  drafts: string;
  notDue: string;
  overdue: string;
  paid: string;
  unpaid: string;
  totals: string;
  self: string;
};
